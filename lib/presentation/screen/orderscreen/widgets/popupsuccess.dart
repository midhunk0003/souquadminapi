import 'package:flutter/material.dart';

void showDilogeshowfun(BuildContext context) {
  showDialog(
    context: context,
    builder: (context) {
      return AlertDialog(
        content: Container(),
      );
    },
  );
}
